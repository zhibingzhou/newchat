package router

import (
	"github.com/gin-gonic/gin"
)

func InitJwtRouter(Router *gin.RouterGroup) {
	// ApiRouter := Router.Group("auth")
	// {
	// 	ApiRouter.POST("logout", v1.JsonInBlacklist) // jwt加入黑名单
	// }
}
